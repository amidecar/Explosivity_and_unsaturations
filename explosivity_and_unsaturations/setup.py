from setuptools import setup, find_packages

setup(
      name="explosivity_and_unsaturations",
      version="1.0",
      packages=find_packages(),
      insatll_requires=["rdkit","tkinter","math"]
      )